# Compromised user

Disable or remove affected access, revoke sessions/credentials, and preserve audit evidence. Review organization/project membership changes, exports, authorization changes and sensitive actions. Do not rely on UI-only removal: protected data access must fail server-side/RLS. Restore access only after identity recovery and document the incident actions.