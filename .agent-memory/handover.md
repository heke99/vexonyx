# Handover

Continue from branch `agent/vexonyx-foundation-v3`. Do not rebuild the tenant/RLS foundation unless verification shows a defect. First inspect CI and Vercel deployment. If green, continue file quarantine/retrieval work. Never treat current model manifest entries as production-approved: revisions/licenses remain intentionally unpinned. Do not activate external tool execution until scope, approval, egress, sandbox and kill-switch tests pass.
