# Work plan

1. Foundation + tenant security — implemented, verify CI/advisors.
2. Marketing + waitlist — implemented, verify deployed UI and RPC.
3. Workspace domain — schema + initial UI implemented; expand CRUD workflows.
4. AI framework — provider contracts + registry + mock stream implemented; add durable queue/orchestrator service.
5. Retrieval — implement quarantine worker, chunking, embeddings and tenant-bound search.
6. Small/general/security models — verify source/license, pin revisions, benchmark exact runtimes.
7. Tools + sandbox — only after scope/approval/kill-switch/network tests exist.
8. GLM reasoning escalation — last model tier.
9. Evals, load, DR/restore — required before beta acceptance.
