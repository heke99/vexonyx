import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Activity" description="Correlated activity will combine domain events, agent steps, tool runs and audit provenance." cards={[["EVENTS","Domain stream","Project, file, agent, finding and report events use an outbox-ready architecture."],["CORRELATION","Request IDs","Request, project, generation, run and tool identifiers are designed to correlate across layers."],["REDACTION","Logs","Secret-bearing fields must be redacted before structured logging."]]}/>}
