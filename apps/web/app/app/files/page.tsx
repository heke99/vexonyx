import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Files" description="Binary objects live in private storage; metadata begins quarantined." cards={[["UPLOAD","Quarantine","Direct browser upload paths are tenant-scoped and metadata processing is trusted-server controlled."],["PROVENANCE","Versions","Hashes, parser versions and source metadata preserve where context came from."],["RETRIEVAL","Chunks","File chunks are modeled for tenant-bound pgvector retrieval."]]}/>}
