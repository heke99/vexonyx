import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Team" description="Organization membership is the primary tenant permission boundary." cards={[["ROLE","Owner / admin","Manage membership, authorization and scope."],["ROLE","Member","Work inside tenant projects without changing authorization control-plane state."],["ROLE","Viewer","Read tenant data without mutation access."]]}/>}
