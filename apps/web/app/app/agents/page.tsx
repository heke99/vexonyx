import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Agents" description="Persistent run state, checkpoints, budgets and approval boundaries." cards={[["STATE","Runs","Queued, active, paused and completed runs are represented in the AI schema."],["SAFETY","Approvals","External execution remains disabled until scope, authorization and approval gates are wired."],["RECOVERY","Checkpoints","Run checkpoints and idempotency keys are ready for worker resume logic."]]}/>}
