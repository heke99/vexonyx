import { MockChat } from "@/components/mock-chat";export default function Page(){return <div className="app-content"><div className="app-heading"><div><h1>Chat</h1><p>Mock inference is the active development provider until private inference is wired and evaluated.</p></div></div><MockChat/></div>}
