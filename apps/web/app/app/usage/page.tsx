import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Usage" description="Usage is designed to follow the correct organization, user, project and run." cards={[["METER","AI","Input/output tokens, generations, queue and model cost."],["METER","Execution","Tool runs, sandbox seconds and GPU seconds."],["CAPS","Budgets","Tenant and run hard caps are represented before paid inference is enabled."]]}/>}
