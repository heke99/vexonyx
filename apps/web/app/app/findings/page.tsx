import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Findings" description="Potential findings move through review with immutable evidence history." cards={[["LIFECYCLE","Review state","Potential → reviewing → validated → reported → remediated → verified."],["EVIDENCE","Provenance","Agent, tool and file source IDs can be retained with content hashes."],["QUALITY","Confidence","Severity and confidence remain distinct signals for human review."]]}/>}
