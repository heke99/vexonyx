import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Settings" description="Organization settings will expose controls without leaking control-plane secrets to the browser." cards={[["ACCESS","Membership","Organization membership and future project-specific roles."],["POLICY","Approvals","Configurable approval gates for high-impact operations."],["DATA","Retention","Export, deletion and retention policies are staged for beta readiness."]]}/>}
