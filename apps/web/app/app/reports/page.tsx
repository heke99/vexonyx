import { WorkspacePlaceholder } from "@/components/workspace-placeholder";export default function Page(){return <WorkspacePlaceholder title="Reports" description="Versioned report data is modeled before export workers are enabled." cards={[["STRUCTURE","Technical + executive","Report sections are ready for scope, methodology, risk, findings, evidence and recommendations."],["VERSIONS","Snapshots","Report versions preserve prior output rather than mutating history silently."],["EXPORT","Workers","PDF, DOCX, Markdown and JSON export belong in isolated workers, not Vercel request handlers."]]}/>}
