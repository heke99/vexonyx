import { ContentPage } from "@/components/content-page";export default function Page(){return <ContentPage label="LEGAL" title="Acceptable use" intro="Active security execution must be tied to a defined engagement, valid authorization and normalized target scope." cards={[{title:"Permission required",body:"Users must have authorization for targets before active external assessment steps are enabled."},{title:"Control-plane enforcement",body:"VEXONYX scope and authorization checks are designed to be independent of model instructions or retrieved content."}]}/>}
