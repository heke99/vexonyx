import Link from "next/link";import { ContentPage } from "@/components/content-page";
export default function Page(){return <><ContentPage label="PRICING" title="Private beta first." intro="VEXONYX pricing is not published yet. Usage, entitlements, quotas and hard cost caps are already represented in the platform architecture." cards={[{title:"Usage-aware",body:"Generations, tokens, tool runs, sandbox time, GPU time, storage and exports can be metered per tenant."},{title:"Hard caps",body:"Organization and run budgets are designed to pause runaway spend instead of producing surprise bills."}]}/><div className="shell" style={{paddingBottom:80}}><Link className="button" href="/waitlist">Join the waitlist</Link></div></>}
