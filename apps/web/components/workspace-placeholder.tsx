export function WorkspacePlaceholder({ title, description, cards }: { title: string; description: string; cards: Array<[string,string,string]> }) {return <div className="app-content"><div className="app-heading"><div><h1>{title}</h1><p>{description}</p></div></div><div className="placeholder-grid">{cards.map(([label,name,body])=><article className="placeholder-card" key={name}><span>{label}</span><h2>{name}</h2><p>{body}</p></article>)}</div></div>}
